<p style="color: green"><%= notice %></p>

<%= render @fornecedor %>

<div>
  <%= link_to "Editar", edit_fornecedor_path(@fornecedor) %> |
  <%= link_to "voltar", fornecedors_path %>

  <%= button_to "Deletar", @fornecedor, method: :delete, data: { confirm: "Tem certeza que deseja deletar este fornecedor?" } %>

</div>

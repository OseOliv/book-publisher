<p style="color: green"><%= notice %></p>

<%= render @fornecedor %>

<div>
  <%= link_to "Editar", edit_fornecedor_path(@fornecedor) %> |
  <%= link_to "Voltar", fornecedors_path %>
  <%= button_to "Deletar", @fornecedor, method: :delete, data: { turbo_confirm: "Tem certeza que deseja deletar este fornecedor?" } %>

</div>
